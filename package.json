{
  "name": "snowflow-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "SNOWFLOW open-world foundation — client (Babylon/WebGPU) + Colyseus server + shared contracts",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @snowflow/client",
    "dev:client": "npm run dev -w @snowflow/client",
    "dev:server": "npm run dev -w @snowflow/server",
    "dev:all": "npm run dev:server & npm run dev:client",
    "build": "npm run build -w @snowflow/shared && npm run build -w @snowflow/client && npm run build -w @snowflow/server",
    "build:client": "npm run build -w @snowflow/client",
    "build:server": "npm run build -w @snowflow/server",
    "build:shared": "npm run build -w @snowflow/shared",
    "typecheck": "npm run typecheck -w @snowflow/shared && npm run typecheck -w @snowflow/server",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "check": "npm run build:shared && npm run typecheck && npm run build:client && npm run build:server",
    "clean": "npm run clean --workspaces --if-present && rimraf node_modules packages/*/node_modules packages/*/dist",
    "redis:up": "wsl -e bash -lc \"cd /mnt/c/Users/WHITEPERx/Documents/GitHub/snowflow_demo && docker compose up -d redis\"",
    "redis:down": "wsl -e bash -lc \"cd /mnt/c/Users/WHITEPERx/Documents/GitHub/snowflow_demo && docker compose stop redis\"",
    "redis:logs": "wsl -e bash -lc \"cd /mnt/c/Users/WHITEPERx/Documents/GitHub/snowflow_demo && docker compose logs -f redis\"",
    "redis:ping": "wsl -e bash -lc \"docker exec snowflow-redis redis-cli ping\""
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
